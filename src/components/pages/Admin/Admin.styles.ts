export const adminStyles = /* css */ `
.admin-shell {
  display: grid;
  gap: 1rem;
  margin-inline: auto;
  max-width: 64rem;
  min-height: 100dvh;
  padding: var(--page-gutter);
}

.admin-shell h2 {
  color: var(--heading-colour);
  line-height: 1.1;
  margin: 0;
}
`;
