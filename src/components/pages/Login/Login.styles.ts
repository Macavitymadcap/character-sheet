export const loginStyles = /* css */ `
.auth-page-shell {
  max-width: 58rem;
}

.auth-shell {
  align-items: center;
  display: grid;
  min-height: 22rem;
}

.auth-copy {
  color: var(--muted-text-colour);
  font-weight: 700;
  line-height: 1.45;
  margin: 0;
}
`;
